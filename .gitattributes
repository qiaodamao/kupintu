# 统一换行符，避免 Windows / Linux 协作时整文件 diff
* text=auto eol=lf

# 二进制不参与换行转换与 diff
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary

# 锁文件不进 diff / 不参与语言统计
package-lock.json -diff linguist-generated=true
